[pytest]
addopts = -v --tb=short --strict-markers
python_files = test_*.py
testpaths = tests
markers =
    smoke: basic application availability tests
    login: login and logout tests
    patient_search: patient search and profile tests
    registration: patient registration tests, disabled by default for public demo safety
    o2: tests for OpenMRS Reference Application 2.x
